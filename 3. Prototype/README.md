# Prototype

Creating new objects by copying an existing object (prototype), instead of instantiating new ones via class constructors.

## Why

If object creation is expensive/complex, it can be better to copy an existing object and just modifying the state (updating existing/adding new fields).

## Use case examples

## Pitfalls

## Implementation

## Example

## Variations

